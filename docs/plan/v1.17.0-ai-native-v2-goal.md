# v1.17.0 AI Native v2 — Goal 验收追踪表（targets 模式）

> 契约来源：`docs/plan/v1.17.0-ai-native-v2.md`
> 视觉基准：`docs/plan/evidence/v1.17.0-ai-native-v2.html`
> 建立时间：2026-09-13
> 规则：**任何一项都不得凭「已尽力」标记完成；完成必须有对应证据。**

---

## 0. 前置事实

| 项 | 值 |
| --- | --- |
| 分支 | `release/orion-studio-v1.17.0` |
| 起始 HEAD | `09d54f9f`（v1 Task Inspector + 中心会话 surface） |
| 构建 | `LK_CUSTOM_WEBRTC=~/.cache/orion-webrtc/prebuilt/mac-arm64-release`（详见 2026-09-12 记忆） |
| 测试基线 | `cargo test --target aarch64-apple-darwin -p agent_ui --features test-support --lib` → **437 passed / 0 failed** |
| 已安装 | `/Applications/Orion Studio Dev.app`（含 v1 改动；**注意：v1 曾因面板优先级冲突启动崩溃，已修**） |

### 0.1 已确认的产品决策（Phase 0-4）

**`Open in Code Workspace` 的默认回退布局 = Agentic**（用户 2026-09-13 确认）。
后续阶段不得自行更改；实现落点为 `WindowLayout::Agent(None)`，集中在 `open_in_code_workspace`。

### 0.2 环境约束（已知）

- 沙箱**无屏幕录制权限**（`screencapture` 返回全黑）、**无 Apple Events 权限**（System Events -10004）、`open -a` 无法让 GUI 进程驻留 → **Phase 4 与 §8.2 的真机截图验收无法在沙箱内完成**，需用户在沙箱外执行。
- 交付前需按 `AGENTS.md` 在 `README.md` 前两行保留人工评审标记（当前已存在）。

---

## 1. 分组验收项

图例：⬜ 未开始 · 🔄 进行中 · ✅ 达标（附证据） · ⛔ 阻塞（附原因）

### P1. 去 Tab 的导航契约（Phase 1，计划要求先串行）

| ID | 验收项 | 状态 | 证据 |
| --- | --- | --- | --- |
| P1-1 | 中心 pane tab bar 改为**按 `WindowLayout::AiNative` 判断**，不再按 active item | ✅ | `ai_native_conversation_item.rs::sync_center_tab_bar` 重写：`if ai_native { set_should_display_tab_bar(\|_,_\| false) } else { 恢复 TabBarSettings predicate }`，删除了 `active_item().act_as::<AiNativeConversationItem>()` 判断 |
| P1-2 | 进入 AI Native 始终聚焦当前中心会话；无 thread 时进入带 Agent Chooser 的空草稿 | 🔄 | 聚焦已由 `use_ai_native_layout` 第 5 步保证；**Agent Chooser 部分属 Phase 2，未做** |
| P1-3 | 定义并实现 `OpenInCodeWorkspace`；文件/review 只有这一个合法出口 | ✅ 动作层 | `workspace.rs` 新增 action；`ai_native_conversation_item.rs::open_in_code_workspace` 落 `WindowLayout::Agent(None)`。测试 `test_open_in_code_workspace_leaves_for_the_agentic_layout` **通过**（含「离开不得拆掉持有任务状态的 panel」断言） |
| P1-4 | Classic / Agentic 保持原 predicate；离开 AI Native 后用户 items 完整恢复 | ✅ | `sync_center_tab_bar` 的 else 分支恢复 `TabBarSettings::get_global(cx).show`；`sync_ai_native_surface` 首行即调用它，切走时自动恢复 |
| P1-5 | AI Native 内打开 code item **不再**带回 tab row（v1 漏洞） | ✅ | 改写断言 `test_ai_native_owns_the_center_surface_and_the_inspector`：`AgentDiffPane::deploy_in_workspace` 后断言 tab bar **仍为隐藏**（原断言要求「必须恢复」）。**通过** |

### P2. 统一的新会话 Agent Chooser（Phase 2）

| ID | 验收项 | 状态 | 证据 |
| --- | --- | --- | --- |
| P2-1 | 抽取 `NewThreadChoice` / `available_new_thread_choices()`（无 UI 副作用） | ✅ 代码 | `agent_panel.rs:5115` 新增 `pub(crate) enum NewThreadChoice { Native{display_name}, External{agent_id, display_name, icon_path}, Terminal }` + `agent()` / `label()`；`AgentPanel::available_new_thread_choices(&self, cx: &App)` 只读 agent server store + ACP registry，**不改任何状态**；顺序 = Native → External（按名字排序）→ Terminal（`supports_terminal` 为真时）。编译通过 |
| P2-2 | 创建入口带 `persist_selection` 参数，旧 toolbar 行为不变 | ✅ 代码 + 测试 | `AgentPanel::start_new_thread(choice, persist_selection, workspace, window, cx)` 已实现且**已被 toolbar 使用**。`render_toolbar` 里那段 170 行 `ContextMenu::build` 闭包**整体重写为渲染 `available_new_thread_choices()`**：Native → Terminal → External Agents 分组 → Add More Agents（保持原分组与顺序）。新增模块级 `start_new_thread_from_menu()` 统一派发。回归测试 `test_available_new_thread_choices_offers_native_and_terminal` **通过**；`dead_code` 警告清零。**全量 439 passed / 0 failed**，fmt ✅ |
| P2-3 | AI Native 左侧 New conversation chooser 复用同一模型 | ⬜ | |
| P2-4 | Native / Orion Code / external ACP / Terminal / disabled / auth / collaboration / Add more agents | ⬜ | |
| P2-5 | 已有消息不可换 Agent，只能新开线程 | ⬜ | |
| P2-6 | 临时选择不污染全局默认或无关线程 | ⬜ | |

### P3. TaskEnvironmentPanel（Phase 3）

| ID | 验收项 | 状态 | 证据 |
| --- | --- | --- | --- |
| P3-1 | `TaskInspectorPanel` → `TaskEnvironmentPanel`，panel key / dock 恢复迁移 | ✅ 代码 | 模块与文件整体改名：`task_inspector.rs` → `task_environment.rs`、`task_inspector/` → `task_environment/`（`git mv`，保留历史）；类型 `TaskInspectorPanel`→`TaskEnvironmentPanel`、action `ToggleTaskInspector`→`ToggleTaskEnvironment`、`sync_task_inspector`→`sync_task_environment`、actions! 命名空间 `task_inspector`→`task_environment`。**迁移策略：`panel_key()` 的字符串值刻意保持 `"task_inspector_panel"` 不变**，使 v1 保存的布局仍能恢复本面板，不会出现空 dock 或重复面板；新增测试 `test_environment_panel_is_tab_less_and_keeps_its_dock_key` 锁定该值 |
| P3-2 | **删除** `TaskInspectorTab`、`ToggleButtonGroup`、`ShowTask*` action、palette filter、相关测试 | ✅ | 删除 `enum TaskInspectorTab` + `index()`、`active_tab` 字段、`active_tab()`/`set_active_tab()`、`render_tab_strip()`（含 `ToggleButtonGroup` 三键）、`show_tab()`、3 个 `ShowTask*` action 及其注册与 palette `TypeId`、`TAB_STRIP_HEIGHT` 常量；**删除 `task_environment/files.rs` 与 `review.rs` 两个 Tab 视图**（渲染责任消失）；测试改写：`test_inspector_defaults_to_changes_and_switches_tabs` → `test_environment_panel_is_tab_less_and_keeps_its_dock_key`，`test_inspector_commands_are_hidden_when_ai_is_disabled` → `test_environment_command_is_hidden_when_ai_is_disabled`（只断言 1 个 action）。**零警告，clippy EXIT=0** |
| P3-3 | 统一 vertical disclosure row API（7 行） | ✅ **7/7 全部实现** | 七行齐备，顺序按计划 §4.6。**Changes**：`+N −N`（task-touched 求和）+ 可点击文件行 + `Open review in Code Workspace` CTA。**LocalWorktree**：`Worktree::abs_path()`，无 project 显式 unavailable。**Branch**：`RepositorySnapshot.branch: Option<Branch>`（**此前误判不可得** —— 它在 `project::git_store` 的 snapshot 上，不在 `git` crate），`ref_name` 去 `refs/heads/` 前缀，`UpstreamTracking::Tracked(status).{ahead,behind}`；**无 Git 整行隐藏**。**CommitOrPush**：说明 + 不复制 Git 表单。**Plan**：`AcpThread::plan().stats()` → `completed / len()`，plan 为空整行隐藏。**Subagents**：遍历 `entries()` 中带真实 `subagent_session_info` 的 `ToolCall`，显示 `label` + Done/Running（**状态以文字表达，不只靠颜色**）。**Sources**：`AgentPanel::active_conversation_view()` → `root_thread_view()` → `ThreadView.message_editor` → `MentionSet::mentions()`，`MentionUri` 按变体映射为可读标签（**该枚举无 `Display`**） |
| P3-4 | 从 `changes.rs` / `files.rs` / `review.rs` 抽只读 facts adapter，删 Tab 渲染 | ✅ **完成** | **新建 `task_environment/facts.rs`（106 行）**，只含 4 个只读函数：`task_touched_paths`、`all_changed_entries`、`diff_stat_for`、`status_letter_and_color`。**删除 `changes.rs`（676 行）** —— 其中的 `TaskChangesView` 及其 `Render` / `Focusable` impl、`render_toolbar` / `render_empty_state` / `render_row` / `render_preview` / `render_diff_stat` / `render_patch` / `diff_line`、`ChangeScope` / `Selection` / `Row` 全部移除。面板 `changes_view` 字段与 `notify_children()` 一并删除（改名 `refresh()`，只做 `cx.notify()`）。`agent_ui.rs` 的 `TaskChangesView` re-export 移除。<br>**本项同时消灭了 P3-5 的违规源头**：`changes.rs::select_and_open` 那个「点击文件**同时打开为普通 code item**」的 v1 行为随文件一起消失，AI Native 内已无任何路径会激活中央 editor。三个 Tab View（changes / files / review）现已全部不存在 |
| P3-5 | 局部右侧 diff preview + `Open review in Code Workspace`；**验证无一处激活中心 code item** | ✅ 完成 | **新增 `task_environment/diff_preview.rs`**（`render_patch` + `diff_line`，从 v1 `task_inspector/changes.rs` 的 git 历史恢复后适配）：从任务自己的 `BufferDiff` 渲染 base→current 的紧凑 unified 预览，**120 行截断**，无 hunk 动作 / 无选择 / 无第二 diff 状态机。面板 `task_diff()` 从**活动 thread 的 action log** 按 `worktree_id + path` 找 `(Buffer, BufferDiff)`，所以预览只能显示本任务改的东西。Changes 行：点文件 → `toggle_preview` 选中并高亮 → 其下以 1px 分隔线渲染只读预览；底部 `Open review in Code Workspace` CTA。**无一处激活中心 code item**：预览是纯渲染（`render_patch(&buffer, &diff, cx)`），不触碰 pane / item / focus，编辑只能走显式出口 |
| P3-6 | 空 / unavailable 状态；不伪造 Plan / Subagents / 变更计数 | ✅ | Plan / Subagents / Sources / Branch 四行均在无真实数据时**整行 `continue`（隐藏）**；Local worktree 与 Changes 显示明确 unavailable 文案；无一行渲染假零值或空 branch |
| **P0-C** | 代码工作区回退闭环（`OpenInCodeWorkspace` ⇄ `ReturnToAiNativeTask`） | ✅ 全部完成 | **B1** `workspace.rs` 新增 `ReturnToAiNativeTask`；`return_to_ai_native_task()` 写回 `WindowLayout::AiNative` + `deploy_in_workspace`。<br>**B2** 环境面板 Back to task 改派该 action。<br>**B3 确认层**：`workspace.rs` 新增 **`ConfirmExitToCodeWorkspace`**，与 `OpenInCodeWorkspace` **拆开** —— 后者只 `toggle_modal` 弹 `ExitToCodeWorkspaceModal`（`ModalView` + `AlertModal`），**不写任何 layout 状态**，因此「取消零副作用」是**结构保证**而非约定；前者才真正切 layout。<br>**B4** `title_bar.rs` 的 layout 菜单新增 `Return to task`（`ai_enabled && !is_ai_native` 时可见）。<br>测试：`test_open_in_code_workspace_confirms_before_leaving`（先断言**弹确认不改 layout**，再断言确认后切 Agentic）+ `test_return_to_ai_native_task_restores_layout_and_task` |
| **P0-A** | AI Native 共享 Agent Chooser | ✅ 代码与测试完成（⌘N 归 C1） | **抽共享菜单**：`agent_panel.rs` 新增 **`build_agent_choice_menu(workspace, persist_selection, focus_handle, window, cx)`** 与 **`start_new_thread_with_persistence(...)`**；`start_new_thread_from_menu`（toolbar）改为委托，AI Native 空状态与任务 header 都渲染它 —— **三个入口不可能再分叉**。<br>**A1 ✅** 空状态不再直派 `NewThread`，改用 chooser。<br>**A2 ✅** `render_task_header`（`conversation_view.rs`）的 agent 名变成 **badge**：thread 为空草稿（`entries().is_empty()`）时可点开 chooser（`persist_selection=false`），`anchor=BottomLeft`。<br>**A3 ✅（按设计由 header+composer 现状覆盖）** composer 的状态层已渲染 agent/model/mode 控件；`conversation_view.rs:688` 的注释明确「§5 允许 header **或** composer 状态层，同一实体渲染两次正是 §4.5 所禁」—— 故不另加重复 chip。<br>**A4 ✅** collab 工作区禁用 external agent 时**把原因写在标签上**（`"… — only the native agent is available in this workspace"`），不再只是默默禁用。<br>**A5 ✅** 已有消息后 badge 变 **disabled button + tooltip**（`Tooltip::with_meta`，"This task's agent is fixed. Start a new conversation to use a different agent."）—— 不允许静默换 Agent。<br>**A6 ⏸️** `⌘N` 的落点是**左侧任务轨的 New conversation 主按钮（C1，尚未建）**，归 C1 实现；本阶段已完成空状态按钮与 header badge 两入口共用同一 chooser。<br>**A7 ✅** 新增 `test_chooser_pick_persists_only_when_asked`：先把 `selected_agent` 设为 external，再用 `persist_selection=false` 选 Native → **断言 remembered agent 不变**；再用 `true` → **断言变为 NativeAgent**。测试通过 |

### P4. 视觉、键盘与可访问性（Phase 4）

| ID | 验收项 | 状态 | 证据 |
| --- | --- | --- | --- |
| P4-1 | 紧凑 header / 任务轨道 / environment rows / Center timeline / Composer 对齐 | ⬜ | |
| P4-2 | hover / focus / disabled / running / permission / terminal / 窄窗 / 浅色 / 高对比 / reduced motion | ⬜ | |
| P4-3 | 1440×900 与 1100×760 真机截图 | ⛔ | 沙箱缺屏幕录制权限 |
| P4-4 | 快捷键与 command palette、tooltip、帮助一致 | ⬜ | |

### P5. 文档、回归与发布门禁（Phase 5）

| ID | 验收项 | 状态 | 证据 |
| --- | --- | --- | --- |
| P5-1 | 用户文档 / release notes 更新 | ⬜ | |
| P5-2 | `cargo fmt --check` | ⬜ | |
| P5-3 | 作用域 clippy 零警告 | ⬜ | |
| P5-4 | `git diff --check` | ⬜ | |
| P5-5 | 全量 `agent_ui` 测试 | ⬜ | 基线 437 passed |
| P5-6 | 干净 worktree / Dev App 手工验收 | ⛔ | 沙箱无法运行 GUI |

### §8.3 发布阻断条件（全绿才可发布）

| 阻断项 | 状态 |
| --- | --- |
| 中心出现 tab row | ✅ 已消除（P1-1/P1-5） |
| 右侧出现 Changes / Files / Review Tab | ⬜ P3-2 |
| 新会话不能显式选择 Agent | ⬜ P2 |
| 代码预览无提示地覆盖中央对话 | 🔄 出口已建立（P1-3），UI 入口待接（P3-5） |
| 多个位置同时渲染同一个 ConversationView | ✅ v1 已保证（单一实体），未改动 |
| 只通过单元测试、无真实 Dev App 验收 | ⛔ 沙箱限制，需用户执行 |

---

## 2. 进度小结（2026-09-13）

- **Phase 1 全部完成并验证**（P1-1/3/4/5 ✅，P1-2 部分 —— Chooser 归 Phase 2）。
- Phase 2 第一步完成（模型已抽取，**尚未接线**→ 存在 `dead_code` 警告）；Phase 3 / 4 / 5 未开始。
- 本轮新增测试 2 个，改写 1 个；`agent_ui` 全量测试保持绿色。

### 2.1 已安装验证（2026-09-13 18:44）

| 项 | 值 |
| --- | --- |
| 安装路径 | `/Applications/Orion Studio Dev.app` |
| 二进制 | `Contents/MacOS/orion-studio` 1,199,129,872 B **@ Sep 13 18:44:07** |
| 签名 | `Identifier=dev.orion.OrionStudio-Dev`，`Signature=adhoc`，`codesign --verify --deep --strict` ✅ |
| 架构 | `Mach-O 64-bit executable arm64` |
| **启动冒烟** | ✅ **无 panic**（日志为空，越过了 09-12 崩溃的 `initialize_panels` 路径） |
| 已知限制 | 沙箱内进程数恒为 0 —— GUI 应用无法附着窗口服务器，故**无法在此确认窗口渲染**；真机验收需用户执行（见 P4-3 / P5-6） |

安装方式：`cargo build --bin orion-studio` → 覆盖 `Contents/MacOS/orion-studio` → `codesign --force --deep --sign -`（增量约 3 分钟，避免完整 bundle 的 30+ 分钟 git 下载）。

| **D2** | §8.1「Changes/worktree/branch rows 的 data filtering 仅针对活动 thread」 | ✅ 测试 | `test_task_scoped_change_facts_stay_scoped_to_the_active_thread`：① 无活动 thread → `task_touched_paths` **为空**（不伪造）；② 面板存在但草稿未改动 → `change_facts()` **为空**（Changes 行无假数据）；③ 面板产出的每个 fact **必须来自项目真实变更集**（task scope 是真实 Git 事实上的过滤器，不是第二事实源） |
| **D3** | §8.1「AI Native 内不显示 pane tab bar；Classic/Agentic 保持原行为」 | ✅ 测试 | `test_center_tab_bar_follows_the_layout`：① AI Native 外 → `should_display_tab_bar` = 设置值（记下 `outside`）；② 进入 AI Native → **必须隐藏**；③ `ConfirmExitToCodeWorkspace` 离开 → **恢复为 `outside` 原值**。锁定 `sync_center_tab_bar` 的**布局驱动**规则（而非 item 驱动） |

### C 阶段精确拆解（2026-09-16 盘点后写入；C1 是剩余最大块）

| 步骤 | 落点 | 内容 | 风险 |
| --- | --- | --- | --- |
| **C1-a** | `crates/sidebar/src/sidebar.rs`（~8000 行，**Classic/Agentic 共用**） | 现有 New Thread 流程是「选工作区」（`render_new_thread_button` @2480 → `ContextMenu` "New Thread In…" @2569）。AI Native 需要的是「选 Agent」→ 建 draft。**不能改共享菜单的 Classic/Agentic 行为**，需按 layout 分支 | **高**：§8.1 明确要求 Classic/Agentic 无回归 |
| **C1-b** | 同上 | `New conversation` 主按钮（整行可点击）+ 旁边 chevron，**两者都进同一个 chooser**（§3.3）；复用 `agent_panel::build_agent_choice_menu` | 中 |
| **C1-c** | 同上 | **A6 ⌘N**：AI Native 下 `⌘N` 打开同一 chooser。需给 item 加 key_context + `PopoverMenuHandle` + 新 action | 中 |
| **C1-d** | 同上 | 每条任务的状态**文本**（Running / Needs approval / Needs answer / Idle）—— 数据源 `AgentThreadStatus`（sidebar.rs:5649 已有），**不能只靠颜色** | 低 |
| **C1-e** | 同上 | 搜索（已有，9 处）与 Pinned/Recent 的验收核对；窄窗口折叠顺序（先 environment 再 rail） | 低 |
| **C2** | `conversation_view.rs` Center header | header 收敛：task title、真实 agent badge（A2 已做）、运行状态、overflow 菜单 | 中 |
| **C3** | `thread_view.rs` Composer | 来源入口、发送/停止真实状态、`⌘Enter`；**不得复制** header 已有的 agent 信息（§4.5） | 中 |
| **C4** | 全局 | 深/浅主题、高对比、reduced motion、Tab/Shift-Tab、VoiceOver；每个纯图标控件有名称+tooltip+focus+禁用解释 | 低（逐个核对） |
| **C5** | Dev App | §8.2 七项手工验收 + 截图/录屏。**硬阻塞：需要用户环境（屏幕录制权限）** | 阻塞 |

**当前状态**：Phase 1 / P0-A / P0-B / P0-C 代码+测试全绿（443 passed）。C1 起是纯 UI 建设，尚未开始。

### v3 Phase 1 进度（2026-09-16 开始执行 v3）

| v3 条目 | 状态 | 实现 |
| --- | --- | --- |
| **P1.1.1** choice model 唯一 | ✅（v2 已就位） | `available_new_thread_choices()` 已是唯一模型；toolbar / 空状态 / 任务 header 三处共用 `build_agent_choice_menu` |
| **P1.1.2** `Manage agents…` 出口 | ✅ | `build_agent_choice_menu` 末尾新增 `Manage agents…` → 派发**既有** `zed_actions::AcpRegistry`（与 toolbar 的 `Add More Agents` 同一真实出口），**不伪造 agent 条目、不自动安装/认证**。三个入口（toolbar / 空状态 / header badge）因此都有了管理出口 |
| **P1.1.3** 真实连接状态 | ⏳ | collab 禁用已带原因（P0-A A4）；认证/启动失败消息需真实 agent 连接，待 Dev App 场景 |
| **P1.1.4** 持久化边界测试 | ✅ | `test_chooser_pick_persists_only_when_asked`（false 不写 remembered agent / true 写）+ `test_commit_row_hides_without_git` |
| **P1.2.1** 七行结构 + 空则隐藏 | ✅（v2 已就位） | |
| **P1.2.2** `Commit or push` 真实摘要 | ✅ | 不再是 "Not duplicated here"：显示 **upstream / ahead / behind**（无 remote 时 `No upstream configured — this branch has no remote to push to.`）+ **本任务变更数**（`0` 时显式 "No task changes"）+ `Open Git in Code Workspace` 按钮（走 `OpenInCodeWorkspace`，**自带确认层**）。**无 Git 时整行隐藏**（与 Branch 同规则）。`muted_line` 改收 `impl Into<SharedString>` 以容纳运行时文本 |
| **P1.2.3** preview 失败解释 | ✅ | 选中文件但 `task_diff` 无结果时显示 "No task-owned diff for this file — it may have changed outside this task."，不再留空盒 |
| **P1.2.4** Sources 沿用 Composer ownership | ✅（v2 已就位） | 只读展示 Composer 实际 mention；增删仍走 Composer |
| **P1.3.2** Return to task 仅 AI Native 往返可见 | ✅ | `workspace.rs` 新增**瞬态全局** `AiNativeTripActive(bool)` + `set_ai_native_trip_active` / `ai_native_trip_active`（**不持久化**，未初始化等同 false）。`leave_for_code_workspace` 置 true、`return_to_ai_native_task` 置 false。`title_bar.rs` 的 `Return to task` 条目改为 `ai_enabled && !is_ai_native && ai_native_trip_active` —— **只在真能回去的时候出现**。测试：`test_open_in_code_workspace_confirms_before_leaving` 增加 3 处断言（fresh=false / 确认后=true）；`test_return_to_ai_native_task_restores_layout_and_task` 增加 1 处（返回后=false）<br>**坑**：`ContextMenu::build(window, cx, …)` **消耗 cx**，全局读取必须在 build 之前完成（与 `ai_enabled`/`is_ai_native` 同样的先行计算模式）；测试里 `VisualTestContext` 要用 `cx.update(|_window, cx| …)` 转成 `&App` |
| **Phase 0 Dev App 安装** | ⛔ | 全量 build 需 Metal Toolchain（沙箱阻塞），需 build owner 在本机/CI 执行 |
| **Phase 3 文档更新** | ✅ **完成** | 三份用户文档已与新行为对齐：<br>• `docs/src/ai/agent-panel.md`：`### Task Inspector {#task-inspector}` → **`### Task Environment {#task-environment}`**，三 Tab 表格替换为**七行表**（含「无真实数据则隐藏」规则）；`Code on demand` 重写（点文件只在右侧预览、**绝不开 editor**；`Open review in Code Workspace` 先确认；`Return to task` 回同一任务）；AI Native 小节新增 **Agent Chooser** 说明（三入口同一 chooser + `Manage agents…` + 一次性选择不写全局默认 + 已有消息 agent 固定）。<br>• `docs/src/getting-started.md:49`：删除 "Task Inspector with **Changes**, **Files**, and **Review** tabs" 与 "open any file you like and use `FocusAiNativeConversation`"，改为 tab-free Task Environment + 显式进出。<br>• `docs/src/ai/parallel-agents.md:14`：同样改为 tab-free Task Environment。<br>**Markdown 检查**：`check_code_blocks.py` 四份全 OK；`check_md_links.py` 在 agent-panel/parallel-agents 报 8 处死链 —— **均为预先存在**（`#terminal-threads`/`#threads-sidebar`/`#worktree-isolation`/`#automatic-compaction`/`#ai-native`/`#new-thread`，都不在本次改动段落；`#ai-native` 在 HEAD 里不存在却早已被链接）。仍建议后续单独清理 |

**门禁**：`clippy EXIT=0`（agent_ui + title_bar） · `fmt ✅` · **444 passed / 0 failed**（新增 `test_commit_row_hides_without_git`）

---

## v3 Phase 0 完成（2026-09-17）—— 交付断层已关闭

### Metal Toolchain 阻塞解除（环境变化）
`xcrun metal --version` → **32023.883** 可用；外置卷已挂载。此前所有「沙箱阻塞」项随之解锁。

### Phase 0.4：Dev App 构建并覆盖安装 ✅
```sh
./script/bundle-mac -d -i        # debug 构建 + 安装到 /Applications
```
结果：`bundle EXIT=0`，`Installed application bundle: /Applications/Orion Studio Dev.app`

| 项 | 旧 | 新 |
| --- | --- | --- |
| CFBundleVersion | 20260912.081405 | **20260916.230052** |
| CFBundleShortVersionString | 1.17.0 | 1.17.0 |
| BundleIdentifier | dev.orion.OrionStudio-Dev | dev.orion.OrionStudio-Dev |
| 构建来源 | 旧源码 | **当前 dirty worktree（commit 09d54f9f + v2/v3 改动）** |
| 安装时间 | 2026-09-12 | **2026-09-17 07:02** |

**v3 判定的「交付级 0%」根因已移除**：Dev App 现在包含 v2/v3 的全部实现（无 Tab 环境面板、Chooser、确认层等）。

### Phase 0.3 + v3 §5 门禁（全部通过）
```sh
git diff --check              → ✅
cargo fmt --all -- --check    → ✅
./script/clippy               → EXIT=0   ← 全 workspace，此前一直被 Metal 阻塞
cargo test -p agent_ui --lib  → 444 passed / 0 failed
```
修复过程中 clippy（test target）抓到 `task_environment.rs:963` 一处 redundant clone（`cx.entity().clone()` → `cx.entity()`）。

### 注意
`package ID` 已是 `orion-studio`（rebrand 后），不是 `zed` —— 检查/构建命令要用 `-p orion-studio`。

### 证据目录
`docs/plan/evidence/v1.17.0-ai-native-v3/README.md` 已建：8 个场景清单 + 构建身份要求 + 命名约定。**截图/录屏需人工在 Dev App 中执行**（沙箱无窗口/录屏权限）。

### v3 剩余
1. **§6/§8.2 的真实验收截图**（Dev App 已装，需人工操作并按 README 记录）
2. **P1.1.3** 认证/启动失败的真实消息（需真实 agent 连接）
3. **Phase 2**（P1）任务轨 presentation / 响应式 / 无障碍（= v2 C1-C4）
4. **E**：签名/公证/Release —— 仅在产品验收之后
