# v1.17.0 macOS ARM64 — 发布基线证据（迁移比较基准）

> 由 [跨平台正式发布整改计划](cross-platform-release-remediation-plan.md) §8 M0 要求固化。
>
> **本文件是只读证据记录。v1.17.0 的 tag、产物、Release 均不得移动、重签或覆盖。**

## 1. 只读审计结论

来源：`.agents/skills/orion-studio-release/scripts/audit-stable-release.sh`

```sh
audit-stable-release.sh \
  --repo orion-agents/orion-studio \
  --run 35674112184 \
  --tag v1.17.0 \
  --require-public
```

```json
{
  "result": "PASS",
  "repository": "orion-agents/orion-studio",
  "runId": "35674112184",
  "tag": "v1.17.0",
  "runUrl": "https://github.com/orion-agents/orion-studio/actions/runs/35674112184",
  "releaseUrl": "https://github.com/orion-agents/orion-studio/releases/tag/v1.17.0",
  "isDraft": false,
  "assets": [
    "Orion-Studio-aarch64.dmg",
    "SHA256SUMS"
  ]
}
```

`PASS` 覆盖：工作流成功、签名 tag 证据、Developer ID 身份、Team ID、Apple 接受、装订（stapling）、Gatekeeper、校验和、Release 状态、预期资产集合。

## 2. 源码与 tag 身份

| 项 | 值 |
| --- | --- |
| tag | `v1.17.0` |
| tag 类型 | **annotated**（`git cat-file -t` → `tag`） |
| tag commit | `0d36638173451aad2d36931490d0c028e2819ae2` |
| tag commit 是否属受保护 main | ✅ `git merge-base --is-ancestor` 通过 |
| **发布工作流 headSha** | `65376ee1e0517a0d7dc939f9fceebed9821af0cd` |
| 该 SHA 与 `origin/main` 关系 | **相同** —— 发布时的 main HEAD 即工作流 head SHA |
| 整改基线 worktree | 本分支 `xplat-release-remediation`，基于该 SHA |

## 3. Release 状态与资产

| 项 | 值 |
| --- | --- |
| Release 状态 | **公开**（`isDraft: false`，`isPrerelease: false`，Latest） |
| Draft 创建时间 | `2026-09-17T16:03:43Z` |
| 公开发布时间 | `2026-09-22T01:53:49Z` |
| 资产 1 | `Orion-Studio-aarch64.dmg` — 151,898,380 bytes |
| 资产 1 SHA-256 | `954146a3ed8dd9eac25045a104e2fec49da1c3b4d4e391db3112bbc0bfcdb2c5` |
| 资产 2 | `SHA256SUMS` — 91 bytes |

## 4. 已知脆弱性（整改动因的量化依据）

`release_stable_macos.yml` 在该次成功之前的连续运行结果：

| 时间（UTC） | 结果 | headSha |
| --- | --- | --- |
| 2026-09-22T00:59:45Z | **success** | `65376ee1` |
| 2026-09-21T15:48:08Z | cancelled | `13bc2736` |
| 2026-09-21T13:11:35Z | failure | `90aad218` |
| 2026-09-21T09:54:37Z | failure | `1a9bc2da` |
| 2026-09-21T08:58:34Z | failure | `f690f7e9` |
| 2026-09-21T01:02:51Z | failure | `84d659ee` |
| 2026-09-20T16:13:31Z | failure | `80e7d2e7` |
| 2026-09-20T13:29:05Z | failure | `d64c1f38` |

**成功前连续 1 次 cancelled + 6 次 failure。** 这是「发布链路不可复现」的直接证据，也是 §3.1 第 1 条目标的事实基础。

## 5. 本机资源依赖（M1/M2 的整改对象）

成功运行所绑定、且必须在整改中消除的依赖：

| 依赖 | 工作流位置 | 整改项 |
| --- | --- | --- |
| `[self-hosted, macOS, ARM64, orion-studio-macos-release]` | `build_release.runs-on` | M3/M4 |
| `ORION_STUDIO_RELEASE_SOURCE_REPOSITORY`（本机 Git 源仓库） | `build_release.env`，并被 "Check out validated release source" 强制校验 | **M1** |
| `ORION_STUDIO_BUNDLED_GIT_ARCHIVE`（外置盘 dugite-native 归档，固定 SHA-256 `f014b290f36d121bbc47b29b556044c4f6a2f6494cf78ed2eacfa501b77363b6`） | `build_release.env`，被 "Verify runner capacity" 强制校验 | **M2** |
| 长期 workspace（`git init "$GITHUB_WORKSPACE"` + `clean -ffdx -e target/`，保留 `target/`） | "Check out validated release source" | **M1** |

## 6. 基线不可变承诺

- 不移动、不重签、不覆盖 `v1.17.0` tag 与其 Release 资产。
- 整改后新平台的产物**只能进入新版本的 Draft**，不得回填 v1.17.0。
- 后续所有平台比较均以本文件 §2 的 `65376ee1…` 为源码基准，以 §3 的 SHA-256 为产物基准。
