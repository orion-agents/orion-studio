# Generated from xtask::workflows::extensions::bump_version within the Orion Studio repository.
# Rebuild with `cargo xtask workflows`.
name: extensions::bump_version
on:
  pull_request:
    types:
    - labeled
  push:
    branches:
    - main
    paths-ignore:
    - .github/**
  workflow_dispatch: {}
permissions: {}
jobs:
  determine_bump_type:
    if: vars.ORION_STUDIO_EXTENSION_ORGANIZATION != '' && startsWith(vars.ORION_STUDIO_EXTENSION_ORGANIZATION, 'orion') && github.repository_owner == vars.ORION_STUDIO_EXTENSION_ORGANIZATION
    runs-on: namespace-profile-2x4-ubuntu-2404
    permissions: {}
    steps:
    - id: get-bump-type
      name: extensions::bump_version::get_bump_type
      run: |
        if [ "$HAS_MAJOR_LABEL" = "true" ]; then
            bump_type="major"
        elif [ "$HAS_MINOR_LABEL" = "true" ]; then
            bump_type="minor"
        else
            bump_type="patch"
        fi
        echo "bump_type=$bump_type" >> $GITHUB_OUTPUT
      env:
        HAS_MAJOR_LABEL: |-
          ${{ (github.event.action == 'labeled' && github.event.label.name == 'major') ||
          (github.event.action == 'synchronize' && contains(github.event.pull_request.labels.*.name, 'major')) }}
        HAS_MINOR_LABEL: |-
          ${{ (github.event.action == 'labeled' && github.event.label.name == 'minor') ||
          (github.event.action == 'synchronize' && contains(github.event.pull_request.labels.*.name, 'minor')) }}
    outputs:
      bump_type: ${{ steps.get-bump-type.outputs.bump_type }}
  call_bump_version:
    needs:
    - determine_bump_type
    if: vars.ORION_STUDIO_EXTENSION_ORGANIZATION != '' && startsWith(vars.ORION_STUDIO_EXTENSION_ORGANIZATION, 'orion') && github.repository_owner == vars.ORION_STUDIO_EXTENSION_ORGANIZATION && (github.event.action != 'labeled' || needs.determine_bump_type.outputs.bump_type != 'patch')
    permissions:
      actions: write
      contents: write
      issues: write
      pull-requests: write
    uses: orion-agents/orion-studio/.github/workflows/extension_bump.yml@main
    secrets:
      app-id: ${{ secrets.ORION_STUDIO_AUTOMATION_APP_ID }}
      app-secret: ${{ secrets.ORION_STUDIO_AUTOMATION_APP_PRIVATE_KEY }}
    with:
      bump-type: ${{ needs.determine_bump_type.outputs.bump_type }}
      force-bump: ${{ github.event_name != 'push' }}
concurrency:
  group: ${{ github.workflow }}-${{ github.ref_name }}-${{ github.ref_name == 'main' && github.sha || 'anysha' }}labels
  cancel-in-progress: true
defaults:
  run:
    shell: bash -euxo pipefail {0}
