#!/bin/sh

# Legacy developer compatibility entry point. Use script/orion-studio-local.
set -eu

script_directory=$(CDPATH= cd -P "$(dirname "$0")" && pwd)
exec "$script_directory/orion-studio-local" "$@"
