# Generated from xtask::workflows::release_nightly
# Rebuild with `cargo xtask workflows`.
name: release_nightly
env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: '1'
on:
  schedule:
  - cron: 0 */4 * * *
  workflow_dispatch: {}
permissions:
  contents: read
jobs:
  check_nightly_tag:
    if: github.repository == 'orion-agents/orion-studio' && github.ref == 'refs/heads/main' && vars.ORION_STUDIO_NIGHTLY_RELEASE_ENABLED == 'true'
    runs-on: namespace-profile-2x4-ubuntu-2404
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        fetch-depth: 0
        persist-credentials: false
        fetch-tags: true
    - name: release_nightly::check_nightly_tag
      run: |
        set -euo pipefail

        if [[ "${GITHUB_REF:-}" != "refs/heads/main" ]]; then
            echo "::error::nightly release must run from refs/heads/main"
            exit 1
        fi
        if [[ ! "${GITHUB_SHA:-}" =~ ^[0-9a-f]{40}$ ]]; then
            echo "::error::GITHUB_SHA is not an immutable commit SHA"
            exit 1
        fi

        git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
        checked_out_sha=$(git rev-parse 'HEAD^{commit}')
        main_sha=$(git rev-parse 'refs/remotes/origin/main^{commit}')
        if [[ "$checked_out_sha" != "$GITHUB_SHA" || "$main_sha" != "$GITHUB_SHA" ]]; then
            echo "::error::nightly source does not match protected main"
            exit 1
        fi
    - id: check
      name: release_nightly::check_nightly_tag
      run: |
        NIGHTLY_SHA=$(git rev-parse "nightly" 2>/dev/null || echo "")
        if [ "$NIGHTLY_SHA" = "$GITHUB_SHA" ]; then
            echo "Nightly tag already points to current commit. Skipping."
            echo "skip=true" >> "$GITHUB_OUTPUT"
        else
            echo "skip=false" >> "$GITHUB_OUTPUT"
        fi
    outputs:
      skip: ${{ steps.check.outputs.skip }}
    timeout-minutes: 5
  run_tests_linux:
    needs:
    - check_nightly_tag
    if: github.repository == 'orion-agents/orion-studio' && github.ref == 'refs/heads/main' && vars.ORION_STUDIO_NIGHTLY_RELEASE_ENABLED == 'true' && needs.check_nightly_tag.outputs.skip != 'true'
    runs-on: namespace-profile-16x32-ubuntu-2204
    env:
      CC: clang
      CXX: clang++
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
    - name: steps::setup_cargo_config
      run: |
        mkdir -p ./../.cargo
        cp ./.cargo/ci-config.toml ./../.cargo/config.toml
    - name: steps::cache_rust_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: rust
        path: ~/.rustup
    - name: steps::setup_linux
      run: ./script/linux
    - name: steps::download_wasi_sdk
      run: ./script/download-wasi-sdk
    - name: steps::setup_node
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
      with:
        node-version: '24'
        check-latest: true
        package-manager-cache: false
    - name: steps::cargo_install_nextest
      uses: taiki-e/install-action@a6b2e2dcd845ddd7f509ce4f3ed3d922b80cc5d9
      with:
        tool: nextest
    - name: steps::clear_target_dir_if_large
      run: ./script/clear-target-dir-if-larger-than 350 200
    - name: steps::setup_sccache
      run: ./script/setup-sccache
      env:
        R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
        R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
        R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        SCCACHE_BUCKET: ${{ vars.ORION_STUDIO_SCCACHE_R2_BUCKET }}
    - name: steps::cargo_nextest
      run: cargo nextest run --workspace --no-fail-fast --no-tests=warn
    - name: steps::show_sccache_stats
      run: sccache --show-stats || true
    - name: steps::cleanup_cargo_config
      if: always()
      run: |
        rm -rf ./../.cargo
    timeout-minutes: 60
    services:
      postgres:
        image: postgres:15@sha256:1b92e7a80c021647bf70f5d3eb66066a998e4f5cf43c07bb9dc9f729782cf88e
        env:
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
        - 5432:5432
        options: --health-cmd pg_isready --health-interval 500ms --health-timeout 5s --health-retries 10
  bundle_linux_aarch64:
    needs:
    - run_tests_linux
    runs-on: namespace-profile-8x32-ubuntu-2004-arm-m4
    env:
      CARGO_INCREMENTAL: 0
      CC: clang-18
      CXX: clang++-18
      ORION_STUDIO_REQUIRE_SENTRY: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        persist-credentials: false
    - name: steps::cache_rust_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: rust
        path: ~/.rustup
    - name: run_bundling::set_release_channel_to_nightly
      run: |
        set -eu
        version=$(git rev-parse --short HEAD)
        echo "Publishing version: ${version} on release channel nightly"
        echo "nightly" > crates/zed/RELEASE_CHANNEL
    - name: run_bundling::validate_release_channel
      run: |
        set -euo pipefail
        channel=$(tr -d '\r\n' < crates/zed/RELEASE_CHANNEL)
        case "$channel" in
            stable)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
                    echo "::error::stable artifacts require an exact vMAJOR.MINOR.PATCH tag"
                    exit 1
                fi
                ;;
            preview)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-pre$ ]]; then
                    echo "::error::preview artifacts require an exact vMAJOR.MINOR.PATCH-pre tag"
                    exit 1
                fi
                ;;
            nightly)
                if [[ "${GITHUB_REF:-}" != "refs/heads/main" ]]; then
                    echo "::error::nightly artifacts must be built from refs/heads/main"
                    exit 1
                fi
                ;;
            *)
                echo "::error::release artifacts cannot use channel '$channel'"
                exit 1
                ;;
        esac
    - name: steps::setup_sentry
      uses: matbour/setup-sentry-cli@3e938c54b3018bdd019973689ef984e033b0454b
      with:
        token: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: steps::setup_linux
      run: ./script/linux
    - name: steps::download_wasi_sdk
      run: ./script/download-wasi-sdk
    - name: ./script/bundle-linux
      run: ./script/bundle-linux
      env:
        ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
        ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
        ORION_STUDIO_SENTRY_ORGANIZATION: ${{ vars.ORION_STUDIO_SENTRY_ORGANIZATION }}
        ORION_STUDIO_SENTRY_PROJECT: ${{ vars.ORION_STUDIO_SENTRY_PROJECT }}
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-linux-aarch64.tar.gz
        path: target/release/orion-studio-linux-aarch64.tar.gz
        if-no-files-found: error
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-remote-server-linux-aarch64.gz
        path: target/orion-studio-remote-server-linux-aarch64.gz
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  bundle_linux_x86_64:
    needs:
    - run_tests_linux
    runs-on: namespace-profile-32x64-ubuntu-2004
    env:
      CARGO_INCREMENTAL: 0
      CC: clang-18
      CXX: clang++-18
      ORION_STUDIO_REQUIRE_SENTRY: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        persist-credentials: false
    - name: steps::cache_rust_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: rust
        path: ~/.rustup
    - name: run_bundling::set_release_channel_to_nightly
      run: |
        set -eu
        version=$(git rev-parse --short HEAD)
        echo "Publishing version: ${version} on release channel nightly"
        echo "nightly" > crates/zed/RELEASE_CHANNEL
    - name: run_bundling::validate_release_channel
      run: |
        set -euo pipefail
        channel=$(tr -d '\r\n' < crates/zed/RELEASE_CHANNEL)
        case "$channel" in
            stable)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
                    echo "::error::stable artifacts require an exact vMAJOR.MINOR.PATCH tag"
                    exit 1
                fi
                ;;
            preview)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-pre$ ]]; then
                    echo "::error::preview artifacts require an exact vMAJOR.MINOR.PATCH-pre tag"
                    exit 1
                fi
                ;;
            nightly)
                if [[ "${GITHUB_REF:-}" != "refs/heads/main" ]]; then
                    echo "::error::nightly artifacts must be built from refs/heads/main"
                    exit 1
                fi
                ;;
            *)
                echo "::error::release artifacts cannot use channel '$channel'"
                exit 1
                ;;
        esac
    - name: steps::setup_sentry
      uses: matbour/setup-sentry-cli@3e938c54b3018bdd019973689ef984e033b0454b
      with:
        token: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: steps::setup_linux
      run: ./script/linux
    - name: steps::download_wasi_sdk
      run: ./script/download-wasi-sdk
    - name: ./script/bundle-linux
      run: ./script/bundle-linux
      env:
        ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
        ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
        ORION_STUDIO_SENTRY_ORGANIZATION: ${{ vars.ORION_STUDIO_SENTRY_ORGANIZATION }}
        ORION_STUDIO_SENTRY_PROJECT: ${{ vars.ORION_STUDIO_SENTRY_PROJECT }}
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-linux-x86_64.tar.gz
        path: target/release/orion-studio-linux-x86_64.tar.gz
        if-no-files-found: error
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-remote-server-linux-x86_64.gz
        path: target/orion-studio-remote-server-linux-x86_64.gz
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  build_static_bwrap_linux_aarch64:
    needs:
    - run_tests_linux
    runs-on: namespace-profile-8x32-ubuntu-2004-arm-m4
    steps:
    - name: steps::cache_nix_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: nix
    - name: run_bundling::build_static_bwrap
      uses: cachix/install-nix-action@02a151ada4993995686f9ed4f1be7cfbb229e56f
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - name: run_bundling::build_static_bwrap
      uses: cachix/cachix-action@0fc020193b5a1fa3ac4575aa3a7d3aa6a35435ad
      with:
        name: ${{ vars.ORION_STUDIO_CACHIX_CACHE_NAME }}
        cachixArgs: -v
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
    - name: run_bundling::build_static_bwrap
      run: nix build nixpkgs#pkgsStatic.bubblewrap -L
    - name: run_bundling::build_static_bwrap
      run: |
        cp result/bin/bwrap bwrap-linux-aarch64
        chmod 755 bwrap-linux-aarch64
        gzip -f --stdout --best bwrap-linux-aarch64 > bwrap-linux-aarch64.gz
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: bwrap-linux-aarch64.gz
        path: bwrap-linux-aarch64.gz
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  build_static_bwrap_linux_x86_64:
    needs:
    - run_tests_linux
    runs-on: namespace-profile-32x64-ubuntu-2004
    steps:
    - name: steps::cache_nix_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: nix
    - name: run_bundling::build_static_bwrap
      uses: cachix/install-nix-action@02a151ada4993995686f9ed4f1be7cfbb229e56f
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - name: run_bundling::build_static_bwrap
      uses: cachix/cachix-action@0fc020193b5a1fa3ac4575aa3a7d3aa6a35435ad
      with:
        name: ${{ vars.ORION_STUDIO_CACHIX_CACHE_NAME }}
        cachixArgs: -v
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
    - name: run_bundling::build_static_bwrap
      run: nix build nixpkgs#pkgsStatic.bubblewrap -L
    - name: run_bundling::build_static_bwrap
      run: |
        cp result/bin/bwrap bwrap-linux-x86_64
        chmod 755 bwrap-linux-x86_64
        gzip -f --stdout --best bwrap-linux-x86_64 > bwrap-linux-x86_64.gz
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: bwrap-linux-x86_64.gz
        path: bwrap-linux-x86_64.gz
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  bundle_mac_aarch64:
    needs:
    - run_tests_linux
    runs-on: namespace-profile-mac-large
    env:
      CARGO_INCREMENTAL: 0
      ORION_STUDIO_REQUIRE_SIGNED_RELEASE: '1'
      ORION_STUDIO_REQUIRE_SENTRY: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        persist-credentials: false
    - name: steps::cache_rust_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: rust
        path: ~/.rustup
    - name: run_bundling::set_release_channel_to_nightly
      run: |
        set -eu
        version=$(git rev-parse --short HEAD)
        echo "Publishing version: ${version} on release channel nightly"
        echo "nightly" > crates/zed/RELEASE_CHANNEL
    - name: run_bundling::validate_release_channel
      run: |
        set -euo pipefail
        channel=$(tr -d '\r\n' < crates/zed/RELEASE_CHANNEL)
        case "$channel" in
            stable)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
                    echo "::error::stable artifacts require an exact vMAJOR.MINOR.PATCH tag"
                    exit 1
                fi
                ;;
            preview)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-pre$ ]]; then
                    echo "::error::preview artifacts require an exact vMAJOR.MINOR.PATCH-pre tag"
                    exit 1
                fi
                ;;
            nightly)
                if [[ "${GITHUB_REF:-}" != "refs/heads/main" ]]; then
                    echo "::error::nightly artifacts must be built from refs/heads/main"
                    exit 1
                fi
                ;;
            *)
                echo "::error::release artifacts cannot use channel '$channel'"
                exit 1
                ;;
        esac
    - name: steps::setup_node
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
      with:
        node-version: '24'
        check-latest: true
        package-manager-cache: false
    - name: steps::setup_sentry
      uses: matbour/setup-sentry-cli@3e938c54b3018bdd019973689ef984e033b0454b
      with:
        token: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: steps::clear_target_dir_if_large
      run: ./script/clear-target-dir-if-larger-than 350 200
    - name: run_bundling::bundle_mac::bundle_mac
      run: ./script/bundle-mac aarch64-apple-darwin
      env:
        ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
        ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
        ORION_STUDIO_SENTRY_ORGANIZATION: ${{ vars.ORION_STUDIO_SENTRY_ORGANIZATION }}
        ORION_STUDIO_SENTRY_PROJECT: ${{ vars.ORION_STUDIO_SENTRY_PROJECT }}
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
        MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        APPLE_NOTARIZATION_KEY: ${{ secrets.APPLE_NOTARIZATION_KEY }}
        APPLE_NOTARIZATION_KEY_ID: ${{ secrets.APPLE_NOTARIZATION_KEY_ID }}
        APPLE_NOTARIZATION_ISSUER_ID: ${{ secrets.APPLE_NOTARIZATION_ISSUER_ID }}
        ORION_STUDIO_MACOS_SIGNING_IDENTITY: ${{ vars.ORION_STUDIO_MACOS_SIGNING_IDENTITY }}
        ORION_STUDIO_MACOS_TEAM_ID: ${{ vars.ORION_STUDIO_MACOS_TEAM_ID }}
        ORION_STUDIO_MACOS_PROVISIONING_PROFILE_BASE64: ${{ secrets.ORION_STUDIO_MACOS_PROVISIONING_PROFILE_BASE64 }}
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: Orion-Studio-aarch64.dmg
        path: target/aarch64-apple-darwin/release/Orion-Studio-aarch64.dmg
        if-no-files-found: error
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-remote-server-macos-aarch64.gz
        path: target/orion-studio-remote-server-macos-aarch64.gz
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  bundle_mac_x86_64:
    needs:
    - run_tests_linux
    runs-on: namespace-profile-mac-large
    env:
      CARGO_INCREMENTAL: 0
      ORION_STUDIO_REQUIRE_SIGNED_RELEASE: '1'
      ORION_STUDIO_REQUIRE_SENTRY: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        persist-credentials: false
    - name: steps::cache_rust_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: rust
        path: ~/.rustup
    - name: run_bundling::set_release_channel_to_nightly
      run: |
        set -eu
        version=$(git rev-parse --short HEAD)
        echo "Publishing version: ${version} on release channel nightly"
        echo "nightly" > crates/zed/RELEASE_CHANNEL
    - name: run_bundling::validate_release_channel
      run: |
        set -euo pipefail
        channel=$(tr -d '\r\n' < crates/zed/RELEASE_CHANNEL)
        case "$channel" in
            stable)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
                    echo "::error::stable artifacts require an exact vMAJOR.MINOR.PATCH tag"
                    exit 1
                fi
                ;;
            preview)
                if [[ "${GITHUB_REF_TYPE:-}" != "tag" || ! "${GITHUB_REF_NAME:-}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-pre$ ]]; then
                    echo "::error::preview artifacts require an exact vMAJOR.MINOR.PATCH-pre tag"
                    exit 1
                fi
                ;;
            nightly)
                if [[ "${GITHUB_REF:-}" != "refs/heads/main" ]]; then
                    echo "::error::nightly artifacts must be built from refs/heads/main"
                    exit 1
                fi
                ;;
            *)
                echo "::error::release artifacts cannot use channel '$channel'"
                exit 1
                ;;
        esac
    - name: steps::setup_node
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
      with:
        node-version: '24'
        check-latest: true
        package-manager-cache: false
    - name: steps::setup_sentry
      uses: matbour/setup-sentry-cli@3e938c54b3018bdd019973689ef984e033b0454b
      with:
        token: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: steps::clear_target_dir_if_large
      run: ./script/clear-target-dir-if-larger-than 350 200
    - name: run_bundling::bundle_mac::bundle_mac
      run: ./script/bundle-mac x86_64-apple-darwin
      env:
        ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
        ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
        ORION_STUDIO_SENTRY_ORGANIZATION: ${{ vars.ORION_STUDIO_SENTRY_ORGANIZATION }}
        ORION_STUDIO_SENTRY_PROJECT: ${{ vars.ORION_STUDIO_SENTRY_PROJECT }}
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
        MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        APPLE_NOTARIZATION_KEY: ${{ secrets.APPLE_NOTARIZATION_KEY }}
        APPLE_NOTARIZATION_KEY_ID: ${{ secrets.APPLE_NOTARIZATION_KEY_ID }}
        APPLE_NOTARIZATION_ISSUER_ID: ${{ secrets.APPLE_NOTARIZATION_ISSUER_ID }}
        ORION_STUDIO_MACOS_SIGNING_IDENTITY: ${{ vars.ORION_STUDIO_MACOS_SIGNING_IDENTITY }}
        ORION_STUDIO_MACOS_TEAM_ID: ${{ vars.ORION_STUDIO_MACOS_TEAM_ID }}
        ORION_STUDIO_MACOS_PROVISIONING_PROFILE_BASE64: ${{ secrets.ORION_STUDIO_MACOS_PROVISIONING_PROFILE_BASE64 }}
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: Orion-Studio-x86_64.dmg
        path: target/x86_64-apple-darwin/release/Orion-Studio-x86_64.dmg
        if-no-files-found: error
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-remote-server-macos-x86_64.gz
        path: target/orion-studio-remote-server-macos-x86_64.gz
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  bundle_windows_aarch64:
    needs:
    - run_tests_linux
    runs-on: self-32vcpu-windows-2022
    env:
      CARGO_INCREMENTAL: 0
      ORION_STUDIO_REQUIRE_SENTRY: '1'
      ORION_STUDIO_REQUIRE_SIGNED_RELEASE: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        persist-credentials: false
    - name: run_bundling::set_release_channel_to_nightly
      run: |
        $ErrorActionPreference = "Stop"
        $version = git rev-parse --short HEAD
        Write-Host "Publishing version: $version on release channel nightly"
        "nightly" | Set-Content -Path "crates/zed/RELEASE_CHANNEL"
      shell: pwsh
      working-directory: ${{ github.workspace }}
    - name: run_bundling::validate_release_channel
      run: |
        $ErrorActionPreference = "Stop"
        $channel = (Get-Content "crates/zed/RELEASE_CHANNEL" -Raw).Trim()
        switch ($channel) {
            "stable" {
                if ($env:GITHUB_REF_TYPE -ne "tag" -or $env:GITHUB_REF_NAME -notmatch '^v[0-9]+\.[0-9]+\.[0-9]+$') {
                    throw "stable artifacts require an exact vMAJOR.MINOR.PATCH tag"
                }
            }
            "preview" {
                if ($env:GITHUB_REF_TYPE -ne "tag" -or $env:GITHUB_REF_NAME -notmatch '^v[0-9]+\.[0-9]+\.[0-9]+-pre$') {
                    throw "preview artifacts require an exact vMAJOR.MINOR.PATCH-pre tag"
                }
            }
            "nightly" {
                if ($env:GITHUB_REF -ne "refs/heads/main") {
                    throw "nightly artifacts must be built from refs/heads/main"
                }
            }
            default {
                throw "release artifacts cannot use channel '$channel'"
            }
        }
      shell: pwsh
      working-directory: ${{ github.workspace }}
    - name: steps::setup_sentry
      uses: matbour/setup-sentry-cli@3e938c54b3018bdd019973689ef984e033b0454b
      with:
        token: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: steps::clear_target_dir_if_large
      run: ./script/clear-target-dir-if-larger-than.ps1 350 200
      shell: pwsh
    - name: run_bundling::bundle_windows::bundle_windows
      run: script/bundle-windows.ps1 -Architecture aarch64
      shell: pwsh
      env:
        ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
        ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
        ORION_STUDIO_SENTRY_ORGANIZATION: ${{ vars.ORION_STUDIO_SENTRY_ORGANIZATION }}
        ORION_STUDIO_SENTRY_PROJECT: ${{ vars.ORION_STUDIO_SENTRY_PROJECT }}
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        AZURE_TENANT_ID: ${{ secrets.AZURE_SIGNING_TENANT_ID }}
        AZURE_CLIENT_ID: ${{ secrets.AZURE_SIGNING_CLIENT_ID }}
        AZURE_CLIENT_SECRET: ${{ secrets.AZURE_SIGNING_CLIENT_SECRET }}
        ACCOUNT_NAME: ${{ vars.AZURE_SIGNING_ACCOUNT_NAME }}
        CERT_PROFILE_NAME: ${{ vars.AZURE_SIGNING_CERT_PROFILE_NAME }}
        ENDPOINT: ${{ vars.AZURE_SIGNING_ENDPOINT }}
        FILE_DIGEST: SHA256
        TIMESTAMP_DIGEST: SHA256
        TIMESTAMP_SERVER: http://timestamp.acs.microsoft.com
        ORION_STUDIO_WINDOWS_PUBLISHER: ${{ vars.ORION_STUDIO_WINDOWS_PUBLISHER }}
        ORION_STUDIO_WINDOWS_APPX_PUBLISHER: ${{ vars.ORION_STUDIO_WINDOWS_APPX_PUBLISHER }}
      working-directory: ${{ github.workspace }}
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: Orion-Studio-aarch64.exe
        path: target/Orion-Studio-aarch64.exe
        if-no-files-found: error
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-remote-server-windows-aarch64.zip
        path: target/orion-studio-remote-server-windows-aarch64.zip
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  bundle_windows_x86_64:
    needs:
    - run_tests_linux
    runs-on: self-32vcpu-windows-2022
    env:
      CARGO_INCREMENTAL: 0
      ORION_STUDIO_REQUIRE_SENTRY: '1'
      ORION_STUDIO_REQUIRE_SIGNED_RELEASE: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        persist-credentials: false
    - name: run_bundling::set_release_channel_to_nightly
      run: |
        $ErrorActionPreference = "Stop"
        $version = git rev-parse --short HEAD
        Write-Host "Publishing version: $version on release channel nightly"
        "nightly" | Set-Content -Path "crates/zed/RELEASE_CHANNEL"
      shell: pwsh
      working-directory: ${{ github.workspace }}
    - name: run_bundling::validate_release_channel
      run: |
        $ErrorActionPreference = "Stop"
        $channel = (Get-Content "crates/zed/RELEASE_CHANNEL" -Raw).Trim()
        switch ($channel) {
            "stable" {
                if ($env:GITHUB_REF_TYPE -ne "tag" -or $env:GITHUB_REF_NAME -notmatch '^v[0-9]+\.[0-9]+\.[0-9]+$') {
                    throw "stable artifacts require an exact vMAJOR.MINOR.PATCH tag"
                }
            }
            "preview" {
                if ($env:GITHUB_REF_TYPE -ne "tag" -or $env:GITHUB_REF_NAME -notmatch '^v[0-9]+\.[0-9]+\.[0-9]+-pre$') {
                    throw "preview artifacts require an exact vMAJOR.MINOR.PATCH-pre tag"
                }
            }
            "nightly" {
                if ($env:GITHUB_REF -ne "refs/heads/main") {
                    throw "nightly artifacts must be built from refs/heads/main"
                }
            }
            default {
                throw "release artifacts cannot use channel '$channel'"
            }
        }
      shell: pwsh
      working-directory: ${{ github.workspace }}
    - name: steps::setup_sentry
      uses: matbour/setup-sentry-cli@3e938c54b3018bdd019973689ef984e033b0454b
      with:
        token: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: steps::clear_target_dir_if_large
      run: ./script/clear-target-dir-if-larger-than.ps1 350 200
      shell: pwsh
    - name: run_bundling::bundle_windows::bundle_windows
      run: script/bundle-windows.ps1 -Architecture x86_64
      shell: pwsh
      env:
        ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
        ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
        ORION_STUDIO_SENTRY_ORGANIZATION: ${{ vars.ORION_STUDIO_SENTRY_ORGANIZATION }}
        ORION_STUDIO_SENTRY_PROJECT: ${{ vars.ORION_STUDIO_SENTRY_PROJECT }}
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
        AZURE_TENANT_ID: ${{ secrets.AZURE_SIGNING_TENANT_ID }}
        AZURE_CLIENT_ID: ${{ secrets.AZURE_SIGNING_CLIENT_ID }}
        AZURE_CLIENT_SECRET: ${{ secrets.AZURE_SIGNING_CLIENT_SECRET }}
        ACCOUNT_NAME: ${{ vars.AZURE_SIGNING_ACCOUNT_NAME }}
        CERT_PROFILE_NAME: ${{ vars.AZURE_SIGNING_CERT_PROFILE_NAME }}
        ENDPOINT: ${{ vars.AZURE_SIGNING_ENDPOINT }}
        FILE_DIGEST: SHA256
        TIMESTAMP_DIGEST: SHA256
        TIMESTAMP_SERVER: http://timestamp.acs.microsoft.com
        ORION_STUDIO_WINDOWS_PUBLISHER: ${{ vars.ORION_STUDIO_WINDOWS_PUBLISHER }}
        ORION_STUDIO_WINDOWS_APPX_PUBLISHER: ${{ vars.ORION_STUDIO_WINDOWS_APPX_PUBLISHER }}
      working-directory: ${{ github.workspace }}
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: Orion-Studio-x86_64.exe
        path: target/Orion-Studio-x86_64.exe
        if-no-files-found: error
    - name: run_bundling::upload_artifact
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: orion-studio-remote-server-windows-x86_64.zip
        path: target/orion-studio-remote-server-windows-x86_64.zip
        if-no-files-found: error
    timeout-minutes: 60
    environment: production
  build_nix_linux_x86_64:
    needs:
    - run_tests_linux
    if: github.repository == 'orion-agents/orion-studio'
    runs-on: namespace-profile-32x64-ubuntu-2004
    env:
      ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
      ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
      ORION_STUDIO_CLOUD_PROVIDER_ADDITIONAL_MODELS_JSON: ${{ secrets.ORION_STUDIO_CLOUD_PROVIDER_ADDITIONAL_MODELS_JSON }}
      GIT_LFS_SKIP_SMUDGE: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
    - name: steps::cache_nix_dependencies_namespace
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        cache: nix
    - name: nix_build::build_nix::install_nix
      uses: cachix/install-nix-action@02a151ada4993995686f9ed4f1be7cfbb229e56f
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - name: nix_build::build_nix::cachix_action
      uses: cachix/cachix-action@0fc020193b5a1fa3ac4575aa3a7d3aa6a35435ad
      with:
        name: ${{ vars.ORION_STUDIO_CACHIX_CACHE_NAME }}
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        cachixArgs: -v
    - name: nix_build::build_nix::build
      run: nix build .#default -L --accept-flake-config
    timeout-minutes: 60
    continue-on-error: true
    environment: production
  build_nix_mac_aarch64:
    needs:
    - run_tests_linux
    if: github.repository == 'orion-agents/orion-studio'
    runs-on: namespace-profile-mac-large
    env:
      ORION_STUDIO_CLIENT_CHECKSUM_SEED: ${{ secrets.ORION_STUDIO_CLIENT_CHECKSUM_SEED }}
      ORION_STUDIO_MINIDUMP_ENDPOINT: ${{ secrets.ORION_STUDIO_SENTRY_MINIDUMP_ENDPOINT }}
      ORION_STUDIO_CLOUD_PROVIDER_ADDITIONAL_MODELS_JSON: ${{ secrets.ORION_STUDIO_CLOUD_PROVIDER_ADDITIONAL_MODELS_JSON }}
      GIT_LFS_SKIP_SMUDGE: '1'
    steps:
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
    - name: steps::cache_nix_store_macos
      uses: namespacelabs/nscloud-cache-action@a90bb5d4b27522ce881c6e98eebd7d7e6d1653f9
      with:
        path: ~/nix-cache
    - name: nix_build::build_nix::install_nix
      uses: cachix/install-nix-action@02a151ada4993995686f9ed4f1be7cfbb229e56f
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - name: nix_build::build_nix::configure_local_nix_cache
      run: |
        mkdir -p ~/nix-cache
        echo "extra-substituters = file://$HOME/nix-cache?priority=10" | sudo tee -a /etc/nix/nix.conf
        echo "require-sigs = false" | sudo tee -a /etc/nix/nix.conf
        sudo launchctl kickstart -k system/org.nixos.nix-daemon
    - name: nix_build::build_nix::cachix_action
      uses: cachix/cachix-action@0fc020193b5a1fa3ac4575aa3a7d3aa6a35435ad
      with:
        name: ${{ vars.ORION_STUDIO_CACHIX_CACHE_NAME }}
        authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
        cachixArgs: -v
    - name: nix_build::build_nix::build
      run: nix build .#default -L --accept-flake-config
    - name: nix_build::build_nix::export_to_local_nix_cache
      if: always()
      run: |
        if [ -L result ]; then
          echo "Copying build closure to local binary cache..."
          nix copy --to "file://$HOME/nix-cache" ./result || echo "Warning: nix copy to local cache failed"
        else
          echo "No build result found, skipping cache export."
        fi
    timeout-minutes: 60
    continue-on-error: true
    environment: production
  update_nightly_tag:
    needs:
    - bundle_linux_aarch64
    - bundle_linux_x86_64
    - build_static_bwrap_linux_aarch64
    - build_static_bwrap_linux_x86_64
    - bundle_mac_aarch64
    - bundle_mac_x86_64
    - bundle_windows_aarch64
    - bundle_windows_x86_64
    if: github.repository == 'orion-agents/orion-studio' && github.ref == 'refs/heads/main' && vars.ORION_STUDIO_NIGHTLY_RELEASE_ENABLED == 'true'
    runs-on: namespace-profile-4x8-ubuntu-2204
    steps:
    - id: generate-token
      name: steps::authenticate_as_orion_automation
      uses: actions/create-github-app-token@f8d387b68d61c58ab83c6c016672934102569859
      with:
        app-id: ${{ secrets.ORION_STUDIO_AUTOMATION_APP_ID }}
        private-key: ${{ secrets.ORION_STUDIO_AUTOMATION_APP_PRIVATE_KEY }}
        owner: ${{ github.repository_owner }}
        repositories: ${{ github.event.repository.name }}
        permission-contents: write
    - name: steps::checkout_repo
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        clean: false
        fetch-tags: true
    - name: release::download_workflow_artifacts
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with:
        path: ./artifacts/
    - name: ls -lR ./artifacts
      run: ls -lR ./artifacts
    - name: release::prep_release_artifacts
      run: |-
        mkdir -p release-artifacts/

        mv ./artifacts/Orion-Studio-aarch64.dmg/Orion-Studio-aarch64.dmg release-artifacts/Orion-Studio-aarch64.dmg
        mv ./artifacts/Orion-Studio-x86_64.dmg/Orion-Studio-x86_64.dmg release-artifacts/Orion-Studio-x86_64.dmg
        mv ./artifacts/orion-studio-linux-aarch64.tar.gz/orion-studio-linux-aarch64.tar.gz release-artifacts/orion-studio-linux-aarch64.tar.gz
        mv ./artifacts/orion-studio-linux-x86_64.tar.gz/orion-studio-linux-x86_64.tar.gz release-artifacts/orion-studio-linux-x86_64.tar.gz
        mv ./artifacts/bwrap-linux-aarch64.gz/bwrap-linux-aarch64.gz release-artifacts/bwrap-linux-aarch64.gz
        mv ./artifacts/bwrap-linux-x86_64.gz/bwrap-linux-x86_64.gz release-artifacts/bwrap-linux-x86_64.gz
        mv ./artifacts/Orion-Studio-x86_64.exe/Orion-Studio-x86_64.exe release-artifacts/Orion-Studio-x86_64.exe
        mv ./artifacts/Orion-Studio-aarch64.exe/Orion-Studio-aarch64.exe release-artifacts/Orion-Studio-aarch64.exe
        mv ./artifacts/orion-studio-remote-server-macos-aarch64.gz/orion-studio-remote-server-macos-aarch64.gz release-artifacts/orion-studio-remote-server-macos-aarch64.gz
        mv ./artifacts/orion-studio-remote-server-macos-x86_64.gz/orion-studio-remote-server-macos-x86_64.gz release-artifacts/orion-studio-remote-server-macos-x86_64.gz
        mv ./artifacts/orion-studio-remote-server-linux-aarch64.gz/orion-studio-remote-server-linux-aarch64.gz release-artifacts/orion-studio-remote-server-linux-aarch64.gz
        mv ./artifacts/orion-studio-remote-server-linux-x86_64.gz/orion-studio-remote-server-linux-x86_64.gz release-artifacts/orion-studio-remote-server-linux-x86_64.gz
        mv ./artifacts/orion-studio-remote-server-windows-aarch64.zip/orion-studio-remote-server-windows-aarch64.zip release-artifacts/orion-studio-remote-server-windows-aarch64.zip
        mv ./artifacts/orion-studio-remote-server-windows-x86_64.zip/orion-studio-remote-server-windows-x86_64.zip release-artifacts/orion-studio-remote-server-windows-x86_64.zip
    - name: release::create_sentry_release
      uses: getsentry/action-release@526942b68292201ac6bbb99b9a0747d4abee354c
      with:
        environment: production
      env:
        SENTRY_ORG: ${{ vars.ORION_STUDIO_SENTRY_ORGANIZATION }}
        SENTRY_PROJECT: ${{ vars.ORION_STUDIO_SENTRY_PROJECT }}
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - name: ./script/upload-nightly
      run: ./script/upload-nightly
      env:
        ORION_STUDIO_NIGHTLY_BUCKET: ${{ vars.ORION_STUDIO_NIGHTLY_BUCKET }}
        DIGITALOCEAN_SPACES_ACCESS_KEY: ${{ secrets.DIGITALOCEAN_SPACES_ACCESS_KEY }}
        DIGITALOCEAN_SPACES_SECRET_KEY: ${{ secrets.DIGITALOCEAN_SPACES_SECRET_KEY }}
    - name: steps::update_tag
      uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b
      with:
        script: |
          github.rest.git.updateRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: 'tags/nightly',
              sha: context.sha,
              force: true
          })
        github-token: ${{ steps.generate-token.outputs.token }}
    timeout-minutes: 60
    environment: production
  notify_on_failure:
    needs:
    - bundle_linux_aarch64
    - bundle_linux_x86_64
    - build_static_bwrap_linux_aarch64
    - build_static_bwrap_linux_x86_64
    - bundle_mac_aarch64
    - bundle_mac_x86_64
    - bundle_windows_aarch64
    - bundle_windows_x86_64
    if: github.repository == 'orion-agents/orion-studio' && github.ref == 'refs/heads/main' && vars.ORION_STUDIO_NIGHTLY_RELEASE_ENABLED == 'true' && failure()
    runs-on: namespace-profile-2x4-ubuntu-2404
    steps:
    - name: release::send_slack_message
      run: 'curl -X POST -H ''Content-type: application/json'' --data "$(jq -n --arg text "$SLACK_MESSAGE" ''{"text": $text}'')" "$SLACK_WEBHOOK"'
      env:
        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_WORKFLOW_FAILURES }}
        SLACK_MESSAGE: '❌ ${{ github.workflow }} failed: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'
    environment: production
concurrency:
  group: release-nightly
  cancel-in-progress: true
defaults:
  run:
    shell: bash -euxo pipefail {0}
